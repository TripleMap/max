import { ScrollBodyDirective } from './scroll-body.directive';

describe('ScrollBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollBodyDirective();
    expect(directive).toBeTruthy();
  });
});

import { GaleryRollerDirective } from './galery-roller.directive';

describe('GaleryRollerDirective', () => {
  it('should create an instance', () => {
    const directive = new GaleryRollerDirective();
    expect(directive).toBeTruthy();
  });
});
